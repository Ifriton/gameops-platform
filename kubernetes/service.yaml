apiVersion: v1
kind: Service
metadata:
  name: gameops-api
  namespace: gameops
  labels:
    app.kubernetes.io/name: gameops-platform
spec:
  selector:
    app.kubernetes.io/name: gameops-platform
  ports:
    - name: http
      port: 8000
      targetPort: http
  type: ClusterIP
